extends layout 
include mixins/movie

block content 
    h1=pageTitle
    form
        input(name="year", placeholder="Filter by year", type="number", min="0")
        input(name="rating", placeholder="Filter by rating", type="number", min="0")
        input(value="Search", type="submit")
    each movie in movies 
        +movie(movie)